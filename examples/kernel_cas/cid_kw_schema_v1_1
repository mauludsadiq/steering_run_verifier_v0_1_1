{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "fard://domain/kernel_witness/1.1/schema.json",
  "title": "KernelWitness v1.1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "witness_version",
    "domain",
    "run",
    "artifacts",
    "space",
    "tolerance",
    "projection",
    "descent",
    "samples",
    "termination"
  ],
  "properties": {
    "witness_version": { "type": "string", "const": "1.1" },

    "domain": {
      "type": "object",
      "additionalProperties": false,
      "required": ["domain_name", "domain_version", "schema_cid", "verifier_cid"],
      "properties": {
        "domain_name": { "type": "string", "const": "kernel_witness" },
        "domain_version": { "type": "string", "const": "1.1" },
        "schema_cid": { "type": "string", "minLength": 8 },
        "verifier_cid": { "type": "string", "minLength": 8 }
      }
    },

    "run": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "cid_run",
        "cid_run_algo",
        "cid_run_source",
        "cid_run_field_blank_value",
        "cid_run_blank_fields"
      ],
      "properties": {
        "cid_run": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
        "cid_run_algo": { "type": "string", "const": "sha256" },
        "cid_run_source": { "type": "string" },
        "cid_run_field_blank_value": { "type": "string", "const": "" },
        "cid_run_blank_fields": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "type": "string" },
          "const": ["run.cid_run"]
        }
      }
    },

    "artifacts": {
      "type": "object",
      "additionalProperties": false,
      "required": ["hash_algo", "items"],
      "properties": {
        "hash_algo": { "type": "string", "const": "sha256" },
        "items": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["cid", "bytes_hash", "media_type", "note"],
            "properties": {
              "cid": { "type": "string", "minLength": 8 },
              "bytes_hash": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
              "media_type": { "type": "string" },
              "note": { "type": "string" }
            }
          }
        }
      }
    },

    "space": {
      "type": "object",
      "additionalProperties": false,
      "required": ["n"],
      "properties": {
        "n": { "type": "integer", "minimum": 1 }
      }
    },

    "tolerance": {
      "type": "object",
      "additionalProperties": false,
      "required": ["eps"],
      "properties": {
        "eps": { "type": "number", "exclusiveMinimum": 0 }
      }
    },

    "projection": {
      "type": "object",
      "additionalProperties": false,
      "required": ["p"],
      "properties": {
        "p": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "number" }
          }
        }
      }
    },

    "descent": {
      "type": "object",
      "additionalProperties": false,
      "required": ["phi"],
      "properties": {
        "phi": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "number" }
          }
        }
      }
    },

    "samples": {
      "type": "object",
      "additionalProperties": false,
      "required": ["x_samples"],
      "properties": {
        "x_samples": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "number" }
          }
        }
      }
    },

    "termination": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind"],
      "properties": {
        "kind": { "type": "string", "enum": ["nilpotence_k", "trace"] },

        "k": { "type": "integer", "minimum": 1 },

        "x_trace": {
          "type": "array",
          "minItems": 2,
          "items": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "number" }
          }
        },

        "h_trace": {
          "type": "array",
          "minItems": 2,
          "items": { "type": "integer", "minimum": 0 }
        }
      },
      "allOf": [
        {
          "if": { "properties": { "kind": { "const": "nilpotence_k" } }, "required": ["kind"] },
          "then": { "required": ["k"] }
        },
        {
          "if": { "properties": { "kind": { "const": "trace" } }, "required": ["kind"] },
          "then": { "required": ["x_trace"] }
        }
      ]
    }
  }
}
